import { createStore } from 'vuex'
import { request } from '../network/request';

export default createStore({
  state: {
    officialList: []
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
